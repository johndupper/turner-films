import React from 'react'

const createModalHTML = film => (
  <div>
    <h1>{film.TitleName}</h1>
    <h1>{film.ReleaseYear}</h1>
    <p>does this work?</p>
  </div>
)

export default createModalHTML
